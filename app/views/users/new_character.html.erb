<%= simple_form_for current_user do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :realms, :required => true %>
    <%= f.input :characters, :required => true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Continue" %>
  </div>
<% end %>